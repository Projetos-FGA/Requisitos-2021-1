import Title from "./Title";

interface TopbarProps{
    title: string
    subTitle: string
    children?: any

}
export default function Topbar(props : TopbarProps){
    return (
        <div>

        </div>
    )
}